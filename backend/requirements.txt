# Runtime-only. Tests live in backend/tests (install pytest locally, not in the image).
# Keep this list small — extra wheels (pandas-at-import, twisted, stripe, …)
# inflate RAM and delay k8s readiness.

# Web / ASGI
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
pydantic>=2.5.0,<3
python-multipart==0.0.32
python-dotenv==1.2.2

# DB
motor==3.3.1
pymongo==4.6.3

# HTTP
httpx==0.28.1
requests==2.34.2

# Vault
cryptography>=42.0.0

# Market data
kiteconnect==5.2.0
yfinance>=0.2.40
pandas>=2.2.0,<3
numpy>=1.26.0
openpyxl==3.1.5
python-dateutil>=2.9.0
pytz
tzdata>=2024.1
